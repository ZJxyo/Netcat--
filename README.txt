A trimmed version of Netcat written in C.
Runs under linux environment.
Supports TCP connections only.
Supports -k, -l, -v, -r, -ai <port>, -w launch options.
Server mode with the -l launch option handles requests with multithreading.
